#pragma once

#include <Eigen/Dense>

namespace icarus
{
    template<typename T, size_t N>
    struct OffsetCalibration
    {
        explicit OffsetCalibration();
        explicit OffsetCalibration();
    };
}
